# ============================================================
#  DREX - AI Desktop Assistant
#  requirements.txt
#
#  INSTALL:
#    pip install -r requirements.txt
#
#  For development (optional extras):
#    pip install -r requirements.txt webrtcvad pvporcupine
# ============================================================

# ── Core Runtime ──────────────────────────────────────────────
customtkinter>=5.2.2
Pillow>=10.0.0
python-dotenv>=1.0.0
requests>=2.31.0
loguru>=0.7.2

# ── AI Provider SDKs ─────────────────────────────────────────
google-genai>=1.0.0
groq>=1.0.0
openai>=1.0.0             # Required by OpenRouter + Cerebras

# ── Voice & Audio ────────────────────────────────────────────
SpeechRecognition>=3.10.0
pyaudio>=0.2.11
pyttsx3>=2.90             # Offline TTS
edge-tts>=6.0.0           # Neural online TTS (optional, recommended)
pygame>=2.5.0             # Audio playback for edge-tts (replaces broken playsound)

# ── Web/API (Vercel deployment) ──────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# ── Utilities ────────────────────────────────────────────────
psutil>=5.9.0
pyperclip>=1.8.0
tabulate>=0.9.0
tqdm>=4.66.0

# ── Optional: Wake Word Detection ────────────────────────────
# pvporcupine>=3.0.0      # Preferred (low CPU)
# openwakeword>=0.4.0     # Fallback

# ── Optional: Voice Activity Detection ───────────────────────
# webrtcvad>=2.0.10       # For VAD support

# ── Optional: Document Extraction ────────────────────────────
# pytesseract>=0.3.10     # OCR
# pymupdf>=1.24.0         # PDF extraction
# pdfplumber>=0.11.0      # PDF parsing (alternative)